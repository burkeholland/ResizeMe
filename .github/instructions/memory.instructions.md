---
applyTo: '**'
---

# Project Context & Memory

- There is a GitHub Action (`.github/workflows/release.yml`) that automatically creates a release when a tag starting with `v*` is pushed.
- Memory: Always rely on the GitHub Action to build and release when a tag is pushed.
